---
- name: OpenShift Alertmanager Test Rulebook
  hosts: all
  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: 5050
  rules:
    - name: Debug
      condition: event.payload is defined
      action:
        debug: 
          msg: "Payload defined!"
