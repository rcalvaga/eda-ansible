---
- name: Zabbix Test Rulebook
  hosts: all
  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: 5000
  rules:
    - name: Debug
      condition: event.payload is defined
      action:
        debug: 
          msg: "Payload defined!"
          
    - name: Zabbix Webhook event
      condition: event.payload is defined
      action:
        debug: 
          msg: "Apache: Service is down!"
